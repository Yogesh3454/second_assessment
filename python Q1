


def print_dict_paths(input_dict):
    output = {}
    for key, value in input_dict.items():
        keys = [key]
        current = value
        while isinstance(current, dict):
            keys.append(list(current.keys())[0])
            current = current[list(current.keys())[0]]
        keys.append(current)
        output[key] = keys
    return output
